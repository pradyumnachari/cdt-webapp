# Copy this file to `secrets.toml` (same folder) and fill in your key.
# `secrets.toml` is gitignored and never committed.
#
# Required only for the Cohort tab's free-form Q&A. Everything else in the
# app runs without a key.

openai_api_key = "sk-..."
